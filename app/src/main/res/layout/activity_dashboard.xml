<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.soccerallianceapp.Dashboard_Activity"
    tools:openDrawer="start"
    android:fitsSystemWindows="true"
    android:id="@+id/dashboard_drawer_layout">

    <include
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:id="@+id/dashboard_navigationview"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/navheader"
        app:itemBackground="@drawable/nav_draw_background"
        app:itemIconSize="28dp"
        app:itemIconTint="@color/nav_draw_item_style"
        app:itemTextAppearance="@style/NavDrawerTexApptStyle"
        app:itemTextColor="@color/nav_draw_item_style"
        android:theme="@style/NavigationTheme"
        android:background="@color/colorNavDrawerBackground"/>
</androidx.drawerlayout.widget.DrawerLayout>